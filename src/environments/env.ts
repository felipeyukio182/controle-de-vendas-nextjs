const env = {
    apiBaseUrl: "http://localhost:3000/api",
    localStorageLoginKey: "cdv-login:token",
    routesList: {
        login: "/login",
        home: "/home",
        people: "/people"
    }
}

export default env;